# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional (only if PROVIDER=openai)
OPENAI_API_KEY=sk-...

# Required for ReminderAgent email features (future — not yet deployed)
# EMAIL_SECRET=your-hmac-secret-here

# Defaults (can override)
PROVIDER=claude
MONTHLY_SPEND_CAP=50
RATE_LIMIT_PER_HOUR=30
SESSION_MESSAGE_CAP=50
